{% extends main_template %}
{% block content %}
    <h1>Simple Plugin Test</h1>

    <table class='table'>
    {%- for item in items -%}
      {% if loop.first %}
        <thead>
          <tr>
            {%- for field in table_fields %}
              <th> {{ field }} </th>
            {%- endfor -%}
            <th>Action</th>
          </tr>
        </thead>
        <tbody>
      {% endif %}
          <tr>
            {%- for field in table_fields %}
              <td> {{ item[field] }} </td>
            {%- endfor -%}
            <td> <a href="{{request.route_path('testplugin_assign',uuid=item[idfield])}} " class="btn btn-primary">Add</a> </td>
          </tr>
    {%- endfor -%}
        <tbody>
    </table>

{% endblock %}
